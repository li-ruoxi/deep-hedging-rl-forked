# pyproject.toml
[project]
name = "deep-hedging-rl"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["data_pipeline", "simulator", "rl_agent"]
